## 1. Rust 後端圖片解析實作

- [x] 1.1 在 `lib.rs` 中獲取當前執行檔所在的資料夾路徑，作為相對路徑的基準
- [x] 1.2 重構 `markdown_to_html_with_css` 函數，使用遍歷 `pulldown-cmark` 事件 (Event) 的方式手動處理 HTML 生成
- [x] 1.3 在事件處理中攔截 `Tag::Image`，當路徑為本地相對路徑時，將其轉換為絕對路徑並讀取圖片資料，編碼為 Base64 Data URL 替換原路徑

## 2. 測試與驗證

- [x] 2.1 於執行檔同目錄下放置測試圖片，在編輯器中使用相對路徑語法引入，驗證 HTML 即時預覽可正常顯示圖片
- [x] 2.2 切換至 PDF 預覽模式，確認產生的高精度 PDF 預覽可正常渲染圖片
- [x] 2.3 執行正式編譯打包，驗證單一免安裝 `.exe` 在同目錄有圖片的情況下，亦能正常運作並完美導出含圖片的 PDF 檔案
