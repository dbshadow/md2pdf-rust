## 1. 專案更名與配置更新

- [x] 1.1 修改根目錄的 `package.json` 的 `"name"` 改為 `"md2pdf-workspace"`，並更新對應的 `workspace` scripts 參數。
- [x] 1.2 修改 `frontend/package.json` 的 `"name"` 改為 `"md2pdf-frontend"`。
- [x] 1.3 修改 `server/package.json` 的 `"name"` 改為 `"md2pdf-server"`。
- [x] 1.4 在專案根目錄下重新執行 `npm install --legacy-peer-deps` 以更新並重設 workspaces 連結。

## 2. 授權與說明文檔建立

- [x] 2.1 在根目錄建立 `LICENSE` 檔案，寫入 2026 年度、由 "md2pdf contributors" 持有的標準 MIT License 內容。
- [x] 2.2 在根目錄覆寫並完整撰寫符合 GitHub 專案格式、中英文對應的 `README.md` 說明文件。
